/** US Letter screenplay page at 96dpi — industry standard dimensions. */
export const SCRIPT_PAGE_WIDTH_PX = 816;
export const SCRIPT_PAGE_HEIGHT_PX = 1056;

export const DEFAULT_PAGE_MARGINS = {
	top: 1,
	bottom: 1,
	left: 1.5,
	right: 1,
} as const;
