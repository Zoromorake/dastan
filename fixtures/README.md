# Test fixtures

Sample scripts for import/export and parser tests. Do not commit copyrighted screenplays.

- `sample-screenplay.fountain` — minimal two-scene script for Fountain/FDX round-trip tests.
- For PDF import tests, generate a small PDF from this fixture locally or use an inline buffer in Vitest.

The repo root `black-panther-2018-2.pdf` is gitignored for local manual testing only and must not be committed.
